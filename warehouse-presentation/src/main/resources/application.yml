server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://db:5432/metalWh
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update